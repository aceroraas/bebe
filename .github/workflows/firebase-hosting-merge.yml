# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Build on merge
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    environment: everimoments
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Debug Environment Variables
        run: |
          echo "Environment name: ${{ env.ENVIRONMENT_NAME }}"
          echo "Environment URL: ${{ env.ENVIRONMENT_URL }}"
          echo "Secret exists check:"
          echo "VITE_FIREBASE_API_KEY exists: ${{ secrets.VITE_FIREBASE_API_KEY != '' }}"
          echo "FIREBASE_SERVICE_ACCOUNT_BEBE_AZ exists: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEBE_AZ != '' }}"
          
      - name: Install Dependencies
        run: npm ci
          
      - name: Build
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
        run: |
          echo "Building with environment variables..."
          env | grep VITE_
          npm run build
